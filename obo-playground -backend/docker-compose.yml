version: '3.8'

services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: obo-user
      POSTGRES_PASSWORD: obo-playground
      POSTGRES_DB: obo_playground
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: